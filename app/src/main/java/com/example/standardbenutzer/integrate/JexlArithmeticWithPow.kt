package com.example.standardbenutzer.integrate

import org.apache.commons.jexl3.JexlArithmetic



/**
 * Created by Standardbenutzer on 23.12.2017.
 */
class JexlArithmeticWithPow(lenient: Boolean) : JexlArithmetic(lenient) {
    /*
    override fun bitwiseXor(left: Any, right: Any): Any {
        val l = toDouble(left)
        val r = toDouble(right)
        return Math.pow(l, r)
    }
    */
}